namespace CikApi.Authorization
{
    public enum CikApiRole
    {
        TestAdmin = -100,
        TestUser = -200,
        Unknow = -100,
        Anonymous =100,
        User = 200,
        Admin = 300
    }
}